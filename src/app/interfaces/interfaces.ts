export interface Chat{
    peticion?: string,
}

export interface RedPeticion{
    coeficiente:number,
    variableA:number,
    variableB:number,
}

export interface RedResultado{
    resultado: number,
}